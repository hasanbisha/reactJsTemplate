const initialState = {
    loading: false
}

const General = (state = initialState, { type, payload }) => {
    switch(type) {
        default:
            return state;
    }
} 

export default General;